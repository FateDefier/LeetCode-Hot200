## 51. 题号：146 -- LRU缓存（简单）
### 51.1 设计
```cpp
class LRUCache {
private:
    struct Node {
        int key, val;
        Node *prev;
        Node *next;
        Node(int k, int v) : key(k), val(v), prev(nullptr), next(nullptr) {}
    };

    int cap;        // capacity
    unordered_map<int, Node*> cache;        // 哈希表存的是 key--指针地址
    Node *head;     // 虚拟头尾结点，便于进行对首尾的操作
    Node *tail;

    // 1.头插
    void addToHead(Node *node) {
        node->next = head->next;
        node->prev = head;
        head->next->prev = node;
        head->next = node;
    }

    // 2.删除
    void removeNode(Node *node) {
        node->prev->next = node->next;
        node->next->prev = node->prev;
    }

    // 3.把当前结点移动到头部(删除+头插)（最近使用）
    void moveToHead(Node *node) {
        removeNode(node);
        addToHead(node);
    }

    // 4.删除尾部结点（最久未使用）
    Node* removeTail() {
        Node *node = tail->prev;
        removeNode(node);
        return node;
    }
public:
    LRUCache(int capacity) {
        head = new Node(0, 0);
        tail = new Node(0, 0);
        cap = capacity;
        head->next = tail;
        tail->prev = head;
    }

    int get(int key) {
        if (cache.find(key) == cache.end()) {
            // key 不存在，返回 -1
            return -1;
        }
        // 如果 key 存在，标记为最近使用并返回对应的值
        Node *node = cache[key];
        moveToHead(node);
        return node->val;
    }

    void put(int key, int value) {
        if (cache.find(key) == cache.end()) {
            // 没找到，插入头部，并加入哈希表
            Node *newNode = new Node(key, value);
            cache[key] = newNode;
            addToHead(newNode);

            // 检测是否超出容量，超出则删除最久未使用
            if (cache.size() > cap) {
                // 链表删除
                Node *removedNode = removeTail();
                // 哈希表也要同步更新
                cache.erase(removedNode->key);
                delete removedNode;
            }
        } else {
            // 找到了，更新 key 对应的 value
            Node *node = cache[key];
            node->val = value;
            moveToHead(node);
        }
    }
};
```

- **知识点**：**哈希表**，**双向链表**；

- **思路**：哈希表 + 双向链表(Dummy Head/Tail)，要求访问(get)和插入删除(put)时间复杂度均为 $O(1)$，访问 -- 哈希表查询结点 $O(1)$，插入删除 -- 双向链表 $O(1)$;

- **关键点**：
1. **必须用双向链表**：单链表删除某节点必须知道前驱，而哈希表只存了当前节点指针，无法 $O(1)$ 获取前驱。
2. **必须加哨兵节点（Dummy Head/Tail）**：避免对“删除头节点”、“删除尾节点”、“空链表插入”做特判。这是链表题的**黄金法则**，能让代码量减半且不易出错。
3. **哈希表存的是“节点指针”而非值**：这样找到节点后可以直接操作链表，无需二次查找。
4. **LRU优先保留最近访问的数据, LFU根据访问频率淘汰最少使用项**；

- **可优化点**：**`get` 和 `put` 中重复哈希查找（性能瑕疵）**


**优化版本**
代码如下：
```cpp
class LRUCache {
private:
    struct Node {
        int key, val;
        Node *prev;
        Node *next;
        Node(int k, int v) : key(k), val(v), prev(nullptr), next(nullptr) {}
    };

    int cap;        // capacity
    unordered_map<int, Node*> cache;        // 哈希表存的是 key--指针地址
    Node *head;     // 虚拟头尾结点，便于进行对首尾的操作
    Node *tail;

    // 1.头插
    void addToHead(Node *node) {
        node->next = head->next;
        node->prev = head;
        head->next->prev = node;
        head->next = node;
    }

    // 2.删除
    void removeNode(Node *node) {
        node->prev->next = node->next;
        node->next->prev = node->prev;
    }

    // 3.把当前结点移动到头部(删除+头插)（最近使用）
    void moveToHead(Node *node) {
        removeNode(node);
        addToHead(node);
    }

    // 4.删除尾部结点（最久未使用）
    Node* removeTail() {
        Node *node = tail->prev;
        removeNode(node);
        return node;
    }
public:
    LRUCache(int capacity) {
        head = new Node(0, 0);
        tail = new Node(0, 0);
        cap = capacity;
        head->next = tail;
        tail->prev = head;
    }

    int get(int key) {
        auto it = cache.find(key);
        // key 不存在，返回 -1
        if (it == cache.end()) return -1;
        // 如果 key 存在，标记为最近使用并返回对应的值
        moveToHead(it->second);
        return it->second->val;
    }

    void put(int key, int value) {
        auto it = cache.find(key);
        if (it == cache.end()) {
            // 没找到，插入头部，并加入哈希表
            Node *newNode = new Node(key, value);
            cache[key] = newNode;
            addToHead(newNode);

            // 检测是否超出容量，超出则删除最久未使用
            if (cache.size() > cap) {
                // 链表删除
                Node *removedNode = removeTail();
                // 哈希表也要同步更新
                cache.erase(removedNode->key);
                delete removedNode;
            }
        } else {
            // 找到了，更新 key 对应的 value
            it->second->val = value;
            moveToHead(it->second);
        }
    }
};
```
- **思路**：**复用迭代器 `it->second`**;

---

## 52. 题号：460 -- LFU缓存（简单）
### 52.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 52.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 53. 题号：24 -- 两两交换链表中的结点（简单）
### 53.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 53.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 54. 题号：19 -- 删除链表的倒数第 N 个结点（简单）
### 54.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 54.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 55. 题号：2 -- 两数相加（简单）
### 55.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 55.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 56. 题号：142 -- 环形链表 II（简单）
### 56.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 56.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 57. 题号：25 -- K 个一组反转链表（简单）
### 57.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 57.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 58. 题号：148 -- 排序链表（简单）
### 58.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 58.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 59. 题号：110 -- 平衡二叉树（简单）
### 59.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 59.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

## 60. 题号：98 -- 验证二叉搜索树（简单）
### 60.1 错误解法
```cpp

```

- **思路**：;

- **错误点**：


### 60.2 正确解法及分析
代码如下：
```cpp

```

- **知识点**：；

- **时间复杂度 -- $O(n)$，空间复杂度 -- $O(n)$**

- **思路**：;

- **关键点**：；

---

